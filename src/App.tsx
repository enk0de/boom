import React from 'react';
import { Result, Button } from 'antd';
import './App.scss';

const App = () => (
	<Result
		title="Boom has been initialized"
		subTitle="The boom project base code will be released as an open source before production."
	/>
);

export default App;
